# Autonomyx Agent Rebrand Notes

This repository is an Autonomyx fork of the original upstream project. Branding in top-level project documentation now uses **Autonomyx Agent** naming.

## Scope of this pass

- Updated `README.md` branding references to Autonomyx Agent naming.
- Updated Autonomyx Phase 1 architecture/data-model/example docs to use Autonomyx naming consistently.
- Preserved existing command compatibility (`hermes` CLI) and upstream technical references where URLs, package names, or migration context still rely on current upstream infrastructure.

## Compatibility note

The runtime command surface remains backward-compatible (`hermes` command), while product identity in docs is presented as Autonomyx Agent.
